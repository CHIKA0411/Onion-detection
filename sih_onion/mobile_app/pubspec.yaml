name: onion_quality_app
description: "AI-Based Onion Quality Assessment & Dispute Resolution Mobile App (SIH26031)"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  camera: ^0.10.5+9
  http: ^1.2.0
  path_provider: ^2.1.2
  shared_preferences: ^2.2.2
  fl_chart: ^0.68.0
  pdf: ^3.10.8
  printing: ^5.13.0
  connectivity_plus: ^5.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
